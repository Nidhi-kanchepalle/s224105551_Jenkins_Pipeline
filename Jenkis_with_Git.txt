pipeline {
    agent any
    
    stages {
        stage('Build') {
            steps {
                echo 'Stage 1: Build'
                echo 'Task: Compile and package the code.'
                echo 'Tool: Maven, Gradle, or Ant'
            }
        }
        
        stage('Unit and Integration Tests') {
            steps {
                echo 'Stage 2: Unit and Integration Tests'
                echo 'Task: Run unit tests and integration tests to validate the code.'
                echo 'Tool: JUnit, pytest, or Selenium'
            }
        }
        
        stage('Code Analysis') {
            steps {
                echo 'Stage 3: Code Analysis'
                echo 'Task: Analyze code quality and adherence to standards.'
                echo 'Tool: SonarQube'
            }
        }
        
        stage('Security Scan') {
            steps {
                echo 'Stage 4: Security Scan'
                echo 'Task: Perform security scans to identify vulnerabilities.'
                echo 'Tool: OWASP Dependency-Check or Snyk'
            }
        }
        
        stage('Deploy to Staging') {
            steps {
                echo 'Stage 5: Deploy to Staging'
                echo 'Task: Deploy the application to a staging environment.'
                echo 'Tool: AWS CodeDeploy or Docker'
            }
        }
        
        stage('Integration Tests on Staging') {
            steps {
                echo 'Stage 6: Integration Tests on Staging'
                echo 'Task: Run integration tests in the staging environment.'
                echo 'Tool: JUnit, pytest, or custom scripts'
            }
        }
        
        stage('Deploy to Production') {
            steps {
                echo 'Stage 7: Deploy to Production'
                echo 'Task: Deploy the application to a production environment.'
                echo 'Tool: AWS CodeDeploy or Docker'
            }
        }
    }
    
    post {
        always {
            echo 'Pipeline completed. Sending notification emails.'
        }
        success {
            emailext(
                to: 's224105551@deakin.edu.au',
                subject: 'Build Success',
                body: 'The pipeline has completed successfully.',
                attachLog: true
            )
        }
        failure {
            emailext(
                to: 's224105551@deakin.edu.au',
                subject: 'Build Failure',
                body: 'The pipeline has failed. Please check the logs.',
                attachLog: true
            )
        }
    }
}

